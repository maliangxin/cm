<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yonyoucloud.fi.cmp.mapper.ReceivebillMapper">

    <update id="updateReceivebill" parameterType="java.util.HashMap">
        UPDATE cmp_receivebill
        <set>
            voucherstatus =
            (
                case when voucherstatus = #{oldvoucherstatus}  then #{voucherstatus}
                else  voucherstatus end
            ),
            settlestatus = #{settlestatus},
            settleuser = #{settleuser},
            settledate = #{settledate},
            dzdate = #{dzdate}
        </set>
        <where>
            id = #{id}
        </where>

    </update>


    <update id="updateReceivebillCode" parameterType="java.util.HashMap">
        UPDATE cmp_receivebill
        <set>
            code = #{code}
        </set>
        <where>
            srcbillid = #{srcbillid}
        </where>

    </update>


    <update id="updatebill" parameterType="java.util.HashMap">
        UPDATE cmp_receivebill
        <set>
            settledate = #{settledate},
            dzdate = #{dzdate}
            <if test = "voucherstatus != null and voucherstatus != ''">
                ,voucherstatus = #{voucherstatus}
            </if>
        </set>
        <where>
            id = #{id}
        </where>

    </update>


    <update id="updatepay" parameterType="java.util.HashMap">
        UPDATE cmp_paybill
        <set>
            settledate = #{settledate},
            dzdate = #{dzdate}
            <if test = "voucherstatus != null and voucherstatus != ''">
                ,voucherstatus = #{voucherstatus}
            </if>
        </set>
        <where>
            id = #{id}
        </where>

    </update>

    <update id="updateSettleStatusByIdes" parameterType="java.util.List">
        UPDATE cmp_receivebill set settlestatus = 1 where id in
        <foreach item="item" index="index" collection="idList" open="(" separator="," close=")">
            #{item}
        </foreach>

    </update>


</mapper>
